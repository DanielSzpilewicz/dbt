version: 2

sources: 
    - name: google_analytics_sample
      database: bigquery-public-data
      schema: google_analytics_sample
      tables:
        - name: ga_sessions_*

models:
  - name: totalPageViews
    columns:
      - name: totalPageViews
        tests:
          - not_null

